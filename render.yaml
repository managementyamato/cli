services:
  - type: web
    name: trouble-management
    env: php
    plan: free
    branch: claude/setup-trouble-management-ersIX
    buildCommand: echo "No build needed"
    startCommand: php -S 0.0.0.0:$PORT
    healthCheckPath: /login.php
    envVars:
      - key: PHP_VERSION
        value: "8.1"
